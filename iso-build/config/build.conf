# ─── NovauOS build configuration ──────────────────────────────────────
#
# Loaded by build.sh. Override via environment variables on the CI runner.

NOVAU_VERSION ?= 0.1.0
NOVAU_CODENAME ?= aurora
NOVAU_ARCH ?= amd64
DEBIAN_MIRROR ?= http://deb.debian.org/debian
